
























//Old code related to useLocalObserver @ mobx
/* const Game = {
  guesses: [] as string[],
  guessCount: 0,

  keyEvents: (ev: KeyboardEvent) => {
    if (ev.key === "Enter") {
      Game.guesses.push("elem");
    }
    console.log(Game.guesses);
    //set state?
    // return to set state???
  },

};

export default Game */





  